describe("Testing sortTFByUser", () => {
  beforeAll(() => {
    /**
     * todo: Declare timeFrames
     * Maybe from a json file
     */
  });

  it("is a sample always passing test", () => {
    expect(true).toBeTruthy();
  });
});
