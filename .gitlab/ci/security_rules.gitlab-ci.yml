license_scanning:
  interruptible: true
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - when: never
  variables:
    LICENSE_FINDER_CLI_OPTS: "--aggregate_paths=frontend backend"

nodejs-scan-sast:
  interruptible: true
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - when: never

semgrep-sast:
  interruptible: true
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - when: never

  
secret_detection:
  interruptible: true
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - when: never
